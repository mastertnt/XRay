<UserControl x:Class="XCommand.TestApp.ContextView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Height="200" Width="150">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border BorderThickness="1" BorderBrush="Gray" Margin="2,2,2,0">
            <ScrollViewer>
                <StackPanel x:Name="mCommands" VerticalAlignment="Bottom" />
            </ScrollViewer>
        </Border>
        <TextBlock x:Name="mContextName" Text="Context Name" Grid.Row="1" Margin="2,0,2,2" />
    </Grid>
</UserControl>
